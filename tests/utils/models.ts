

export type NetworkParameters = {
  rpcUrl: string,
  privateKeys: string[]
}

export type NetworkDetails = {
  rpcUrl: string,
  privateKeys: string[]
}

export type AdapterParameters = {
  port: number,
}